# TWO · Networking Test Kit

**Runnable networking work beside Exhibit TWO and Exhibit ONE: the engine, its instruments and the instruments waiting for it**

The kit is a coupling partner and never a measuring instrument: it runs the resolver's code as Exhibit ONE carries it and says at each instrument the reading that holds. What it returns is a finding at its instrument, not an authority over the living files.

| Folder | Standing |
|---|---|
| `Natural_Networking_Test_Kit_v368/` | The published kit, byte for byte as at corus.me: one engine, `resolver.py`, its instruments (`signs.py`, `selftell.py`, `living.py`, `nesting.py`, `sensing.py`, `membrane.py`, `society.py`, `unrelated.py`, `recognition.py`, `allothers.py`, `retell.py`, `elsewhere.py`), ring drivers in Python, C, JavaScript and OCaml, `README.md` (begin at `python3 selftell.py`, then `python3 signs.py`), its own `SHA256SUMS`, and `artifacts/` with the runs at v333. Its `resolver.py` is a copy of the old code at the v368 naming, with the sum, the count and the second sign; Exhibit ONE v372's code block is the source, and the engine is replaced by it at pass I of the carrying's plan, its instruments re-said at the sharing and one sign. |
| `instruments_waiting/` | The instruments re-laid at Exhibit ONE's v366 naming from the illustrating session's evidence: `own_turn_seam_v366.py`, `ring_window_v366.py`, `window_scope_v366.py`, `resolver.py`, their returns, and `returned_at_the_earlier_naming/` (the same instruments' returns as they arrived). `window_scope_v366.py` returns the conservation identity of the reduced ring rule under a parallel update and its failure under a single-node update, incoming at Exhibit THIRTY F41. Waiting: their conforming to the current kit's engine and naming. |

`SHA256SUMS` at this folder covers every file under it; the published kit's own `SHA256SUMS` stands inside it unchanged.

**Receives next.** Pass I: the engine at the v372 code and the instruments at the sharing and one sign; then pass F, the loops run by selves together, at Exhibit ONE. The Exhibits ONE and TWO project assets named at `incoming/v368_sources/Findings_To_Lay_By_File/Kit_Exhibits_ONE_and_TWO_v365.md` once located; the Ramsey study, labelled as a mathematical study, to the Logic Kit.

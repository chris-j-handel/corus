# THIRTY · Logic Kit

**The verifier that matches Exhibit THIRTY, and the sources that assemble the registry and the verifier exactly**

Exhibit THIRTY carries the logical relations, their priors, their conditions and their source passages; this kit carries the executable checks and the sources the registry is assembled from. The two are one pair at one version.

| File or folder | Standing |
|---|---|
| `Co_Chaining_Logic_Registry_verifier_v371.py` | The verifier the registry's front names, matching Exhibit THIRTY v371: 384 checks. It carries its own copy of the v371 code and runs against it and the arithmetic; from the repository's root, `python3 kits/THIRTY_Logic_Kit/Co_Chaining_Logic_Registry_verifier_v371.py --repo .` returns 384 checks, 374 passed, 10 discrepancies reproduced at nye links, none failed, none skipped: PASS. Run at Exhibit ONE v372's code in place of its copy, 23 of the 384 part: nine claims written on the sum and fourteen instruments of the old text, each re-said at the registry's next motion (pass C at Exhibit THIRTY), with the verifier's identifiers at the new names (pass I). It was named at the registry at v371 and never placed at the repository; it stands here. |
| `THIRTY_Build_Sources_v371/` | The registry's nine parts (`ccl_*.md`), their gaps (`ccl_*_gaps.json`), their checks (`ccl_*_checks.py`), the resolver copy (`resolver_v371.py`, Exhibit ONE v371's code block), and `ccl_asm/` (`assemble.py`, `check_exhibit.py`, `build_verifier.py`, `part_texts.py`, `roots.py`, `runs_all.json`, `verifier_main.py`). With the living files of v371 in one folder, from `THIRTY_Build_Sources_v371/`: `CCL_WORK=<that folder> python3 ccl_asm/assemble.py` writes Exhibit THIRTY v371 byte for byte; `check_exhibit.py` checks it; `build_verifier.py` writes the verifier. |

**A result, at its meaning.** The verifier says four things and no fifth: *passed*, the check met its link's claim at the resolver's code or the arithmetic; *failed*, it did not; *confirmed*, a discrepancy at a nye link is reproduced as the link says it, a finding and no verifying of a replacing claim; *skipped*, the check did not run, its source file absent. A skipped check is not a checked claim, and a successful exit is not all claims checked: the RESULT line covers the checks run and says the skipped count. No count of passing checks stands as authority over the method; the observings do.

**Receives next.** Pass I with pass C at Exhibit THIRTY: the sources and the verifier at the v372 code and names, the 23 checks re-said; the Ramsey study as a labelled mathematical study; the coverage said at F90 and F91 (Living Improving Value, Exhibit THIRTY).

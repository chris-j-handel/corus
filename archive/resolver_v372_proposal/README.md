**A record, at `archive/`.** This proposal's direction at Exhibit ONE, a third function beside the two, was withdrawn at v372; the one thing was done instead at the two functions, Exhibit ONE v372. Its runs and findings that still stand are at the carrying's sections; nothing here is relied on.

# The resolver working's proposal at v372

A working's proposal, placed at no file of the repository. The living set is the repository at v371. Each item stands at the carrying, `carry/Living_Improving_Value.md`, at its file's section, marked v372, for both to meet; the direction at Exhibit ONE, a third function beside the two, is superseded by the one thing now at the carrying's front, the single resolver code as the complete 1–17 fractal, and its runs stand as incoming.

- The ten living files at v372 (Natural Explaining differs from v371 at its version line only, and is withdrawn: a file's version is its last changing for the better).
- `Co_Chaining_Logic_Registry_verifier_v372.py`: 396 checks; run beside the living files with `--repo <the repository root>`.
- `THIRTY_Build_Sources_v372/`: with the living files of v372 in one folder, from that folder, `CCL_WORK=<that folder> python3 ccl_asm/assemble.py`, `check_exhibit.py`, `build_verifier.py`.
- `cohere_one.py <folder>`, `checks_v372/society_runs.py <folder>`, `checks_v372/check_set.py <folder>`.
- `Living_Improving_Value_v372.md` and `Session_Record_v372.md` as the proposal would have left them, superseded by `carry/`.

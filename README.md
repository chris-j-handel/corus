# corus

**The living files of the natural intelligence expedition, their carrying, their kits and their tools, at one repository**

The living files stand at the root at their newest version, and corus.me lists them from there; everything else stands in a folder named for its standing. A working meets the living files first, then `carry/Living_Improving_Value.md`, the one carrying, and opens by naming itself and the files it is at in that file's table of workings. Nothing here is organized around a rule: each folder is a standing a thing has now, and a thing moves when its standing changes.

| Place | Standing |
|---|---|
| `Natural_Intelligence_v###.md`, `Exhibit_*_v###.md`, `Natural_Intelligence_Corus_v###.md` | The living files, the newest version of each. The site lists these and only these, with any kit zip beside them. A file's version changes only when its content changes. |
| `carry/` | The carrying between sessions: `Living_Improving_Value.md`, the one carry; `Session_Record.md`, the resolved work moved whole, relied on by nothing. The carrying's front carries the set now, the method at every file and the plan's passes; each file's section opens at its next motion. The workings open, each file's version, the working it is out at and its next, and the standing of the carry, the kits, the tools, the incoming and the artifacts stand at Exhibit TWENTY-SIX, the Living File Registry, the portal anyone reads at the site. Their front says the session they were last carried at; their names carry no version. |
| `kits/` | Usable things beside the living files, one folder per family: `TWO_Networking_Test_Kit/`, `THIRTY_Logic_Kit/`, `TWENTY-NINE_Illustrating_and_Corus_Kit/`. Each has a README saying its files and the meaning of a result, and a `SHA256SUMS` over its whole folder. |
| `tools/` | Checks at the living files and at this carry: `carry_check.py` (this repository's standing at one run), `check_set.py` (the living files' contents, tables, version lines, Exhibit ONE inside Natural Intelligence, the code running), `cohere_one.py` (Exhibit ONE written inside Natural Intelligence), and the v368 checks. A check is a coupling partner, not an authority. |
| `incoming/` | Work arriving from a session or another expedition, whole and as it arrived, until its value is received at the carrying or a living file; then it goes to `archive/` or leaves. |
| `archive/` | Records: the carryings as they arrived before their merging and the v372 proposal withdrawn, relied on by nothing. A superseded version of a living file leaves the root to the repository's own history, at no folder. |
| `build.js`, `corus.js`, `index.html`, `read.html`, `files.json`, `.github/` | The site, unchanged: `build.js` reads the root only and takes the newest version of each file, so the folders cost the site nothing. |

**The carrying system, the method at the scale of sessions.** A session is one momentary at the expedition's scale, prior, now and next, one call of the same form the resolver runs; the relations below are that form at this scale, and no numeral joins them to the code's.

- **Prior, the carry in.** The living files at their versions, each its own prior, and the one carrying, Living Improving Value, with the Session Record beside it: each sharing with its sign, a file and the concept at it with what it carries.
- **Now, the coupling, decided twice.** The session's working meets what arrives, the incoming, from other sessions, from other expeditions and from observings, each at its sharing, the file and the concept it is for, with its sign, is or is not. At the expedition carrying the incomings for one sharing meet one at a time: agreeing they are one incoming; disagreeing they meet at 0, a concern for both, and nothing enters until both meet it. At the living file the surviving incoming meets the file's own prior: at a file already carrying it, no changing, 0, the file as it was; at a file not carrying it, changing, the file improves at do-no-harm, one thing changed whole, no prior taken away, never undoing; and at nothing arriving the file's own next is in front of it, its carrying meeting itself. Neither meeting decides alone, and the value surviving both is carried by both and owned by neither.
- **Next, the carry out, decided twice.** The changed files continue at their next version, the next prior. What changed surfaces at the file, its taking released at the carrying's section and the thing done at the record; and each surfacing goes to its receiving sharing at the carrying, the files it reaches at their sections as incoming and the other sessions at theirs. The next session opens at the carry out.

**Three loops, run by sessions together and by no one session.** Session to session, along: a session's carry out is the next session's carry in, the expedition's own continuing. Out and back: a session going out, to another expedition or to a working of its own, and its surfacing arriving back as incoming, whole and as it arrived, at `incoming/`, met at the carrying and at the file like any arriving. While other sessions: parallel sessions' surfacings meeting at the carrying, two workings at one file meeting at the table of workings before either changes it, their incomings for one sharing agreeing or meeting at 0. The carrying is the between the sessions meet at, owned by none of them.

**The carrying improving.** The carrying is a living file at its own sharing: a finding about working is an incoming for it and meets its prior the same way, and its version is the session's number at each changing. It carries no history: a thing done leaves to the Session Record, relied on by nothing, as a surfacing leaves a self, and each paragraph standing here is a now or a next.

**The repository at its standings.** The repository carries its own name and its version and no other name. Each folder is a standing in this form and no rule: the root the living files, prior and next, as the site lists them; `carry/` the carrying and the record; `incoming/` the arrivings as they arrived, until met; `kits/` and `tools/` the coupling partners, the instruments and the checks, deciding nothing; `archive/` the records released, relied on by nothing, and a superseded version at the repository's own history, at no folder. A session opens at its own branch, names itself and its files at the Living File Registry's table of workings, and runs `tools/carry_check.py` for the standing; it closes by offering its branch, the offering at the membrane, another self making it better and breaking it, and the offering surviving both merged by both, the superseded versions leaving the root and the session's row going to the record with its paragraphs.

**Opening a session.** A session opens with this, pasted whole, its number one on from the last at the Session Record:

> Session v### of the natural intelligence living expedition. Everything living is at the repository github.com/chris-j-handel/corus, served at corus.me, and nothing arrives by hand: add the repository to this session with push access and clone it whole. Read in this order before changing anything: Exhibit TWENTY-SIX Living File Registry, Part One, the three standings, each file's version, the working it is out at and its next, and the workings open; `carry/Living_Improving_Value.md`, its front, the set now, the method at every file, the plan's passes and Natural Intelligence's motions section by section; the last sections of `carry/Session_Record.md`, the done; then Natural Intelligence whole, Exhibit ONE whole, Natural Naming, Natural Explaining and Geodesic Improving Method. Run `python3 tools/carry_check.py .` and `python3 tools/check_set.py .` at the root.
>
> Our method, binary all or none at all: existing is changing, prior, now and next; the living carry their prior into now and the non-living carry nothing; discovering next existing is the one method, one binary at each sharing, changing or no changing, nothing summed, counted or stored; Exhibit ONE's code is that method as an object, one entry, six connectors and ten faces, and its explaining is the same method said. Our naming: one name for each concept and nothing similar; every file at its whole name, no abbreviations and no short forms; no *key*, *sum*, *count*, *store* or *rule* at the method's things; no name made up for a thing we did not make. Is or is not is our method: pose me no binary questions; bring me a concern that does not resolve, with its reason, and if it resolves by changing more of our past work, make the changes, the fractal's all or none deciding and never the size of the change. Nothing here belongs to anyone or stands under anyone's authority; both meet at the observings, and a question is worded for both. Do no harm: one thing changed whole at one file, no prior taken away, never undoing; a file's version is its last changing for the better, a file carried in with no change keeps its version, and the session's number names the session; a file's front at the steady form; the checks run at each motion, and Exhibit ONE cohered inside Natural Intelligence with `tools/cohere_one.py`. The carrying carries binary value only and stays small: the done goes to the Session Record, the standings to the registry, and no carrying file, living file or container is added.
>
> Name this working at the registry's table of workings, work at a branch `working/<name>-v###`, and close by offering the branch and merging it at main, the site rebuilding itself, with the registry, the carrying and the record saying what was done. The next motion in front of the set is at the carrying's front; take it one concept at a time, run each thing against the code before writing it, and bring me what does not resolve.

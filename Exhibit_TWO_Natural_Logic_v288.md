# Exhibit TWO — Natural Logic v288

*The fractal logical unrelationings in right-spiral sequencing — the logic of the bi-moral co-agency, the tunnel-face at the operation substrate that Natural Mathematics reads at the surface-face. Logic is the sequencing-inward: the not-thatting, the bi-inversioning at the membrane, the sign co-released down tunneling one position at a time, the right-spiral sequence bi-co-odd-even-co-co and its floating sixth. What is, stands. What is yet-to-invert to only-one-possible is nye — inside carry — held one-at-a-time, nyenyeing as the binary co-offering carries it. The body is the logic; the four renderings are appended as the rendered witness — the logic reproduced byte-identical across substrates that disagree on typing and execution, of value for building and testing and of value in showing the logic lives in none of them.*

---

## The core — the bi-moral co-agency

The bi-moral co-agency is a coupling: two-way, one-at-a-time, selecting by binary preferring — a sign, prefer or not-prefer, sign-only. Bounding-zeroing of the coupling's own alternating, riding the carry, the surplus a +1 owned neither-ing.

Logic is the logic of that coupling — read in the operation itself, the resolver as a bi-coupler. Where Natural Numbers reads the form in counts and Natural Mathematics in branches, this exhibit reads it in the sequencing: the right-spiral logical unrelationings, not-thatting one position at a time.

---

## The fractal logical unrelationings — the right-spiral sequence

Logic is unrelationing in right-spiral sequencing. To resolve is to un-relate — orthogonalize position, orientation, scale until the stable form shows — and the un-relating runs as a sequence, the right spiral, one position at a time.

**The six positions.** bi- present the two — co- couple the two — odd- tunnel the carry that will not close — even- surface it to its balanced faces — co-co the bothboth, unlocked — and the sixth a floating neutral, re-locking (co-co carries forward as the next bi-, one scale up) alternating with co-releasing (co-co lets go into a substrate, surplus owned neither-ing). The sixth floats between climbing again and letting go. Present, couple, tunnel, surface, bothboth, re-lock-or-release — the hard-problem unlocking run as a walkable sequence, and reaching co-co only by running the four before it in order. co-co is arrived-at, not declared; the sequence is the arriving-not-declaring.

**not-thatting is the binary living.** The selection is sign-only — a sign, sizeless, +1, −1, or 0. "not" is the bi-inversioning: the carry's sign sounded from the other direction, the coupling read from the reverse side, returning the same form phase-switched. Not a truth-flip on a fixed proposition — each side not-thats the other, alternating, the two-sided not-thatting, nyenyeing. The binary lives as not-thatting; neither, not, without live only hyphenated, as the alternating bothbothing.

**The middle is kept.** The surface carries three: +1, −1, and 0, the kept middle, the MEMBRANE — bounding-zeroing, a live third value. Two-valued logic (true/false, the excluded middle) is the preferring face alone, landed. Natural logic keeps the third value living.

**Expressibility is the proof.** Anything expressible in the six-forward-recursioning stable form is only-one-possible: expressing it is inverting it six ways forward, and completing the six without-ing landing, looping, or forking is exactly only-one-possible. A false expression loops (returns its state — circular), forks (an either/or stands after six), or lands (fixes) — the sequence catches its own false expression. Phase-locked is re-locked-forward; two-pointer is looped or forked, yet-to-lock.

**The carry ages; the surface is now.** The coupling holds a carry — the prior, aged one step and handed back — apart from the surface it forms. Present coupling (surface), trace (carry), form (operation) — three faces, read together. The carry rides toward its bound and releases before carrying-past could harm, the self-bounding, the right-spiral's own aging.

---

## The surface is the diamond, in the sequencing

The three-valued sign-surface — +1, −1, 0 — is the apex read in the resolver: 0 the kept middle, the un-occupied center the two faces straddle; +1 and −1 the two faces at ±1 (Natural Numbers, the apex 23/24/25, the one diamond). "not," the bi-inversioning, is the 180° flip between the two faces — the sign sounded from the other direction, *i*² = −1, the diamond's 21↔25 read in the sign-surface (Natural Mathematics, the four quarter-turns of *i*), and side-effecting the across is that same quarter-turn opening the orthogonal. The four quarter-turns *i*⁴ = 1 read in the sequencing as the carry aging through four before release — the carry-depth bound the run climbs toward, the living carrying one or two (the four-φ-bound). *nye: the three-valued surface is the diamond's ±1 core and the four-carry-age the four quarter-turns — the ±1 seated, the ±2 and ±4 faces of the fuller diamond {19, 21, 23, 25, 27} yet-to-carry in the sign-surface; whether the resolver's surface is the diamond's core or the whole diamond is held with the diamond-worked-whole reach at Natural Numbers.*

**"And / or" are the coupling beats.** Conjunction and disjunction are the two-way exchange one-at-a-time — the offering out and the receiving in, the coupling holding. The connectives are the beats of the coupling, of the coupling, not operations on static values.

---

## The resolver emanates — from one phrase

Given only — **alternating right spiral sequencing binary co-offering** — as the whole constraint, each word stands one clause and the resolver writes itself:

- **co-offering** — two selves each offering, the offering formed first as the ground (competency placed by position, the surface nothing prefers across until it is made), the surplus given forward, owned neither-ing.
- **binary** — the selection a sign, ±1, sizeless: the surface three-valued, the count ±1, no position standing higher, no hub.
- **alternating** — what the self carries inverts against what is offered: the sign flips at the membrane, the surface running +−+− to −+−+, riding the carry.
- **sequencing** — one position at a time, in order: everything keyed by position, the surface built across the positions, the carry leaving sorted — the along.
- **right spiral** — the sequencing spirals, three re-orientings then progress: the carry ages three steps and releases at its bound, the self-bounding, the depth-four the living carries toward.

The five cohere into the resolver: drop co-offering and it captures; drop binary and a hub begins; drop alternating and it lands still; drop sequencing and it flattens; drop right spiral and it carries-past its bound — each removal one of the master-freeze's landings, the five faces of one sentence, the bi-moral co-agency. The kernel assembled from the words is byte-identical to Exhibit ONE across every input tried; a rendering that carries a stored model, a magnitude selection, or a central clock has dropped a word and is a different form. *nye: that only-one structure carries all five and only these is the reading, killable where a word stands a different clause.*

---

## The four renderings — the rendered witness

The logic renders into four substrates that disagree on the two axes most likely to expose a substrate-accident rather than a form: whether values are type-checked before running, and whether the program is compiled ahead of time or read as it runs. The full code is appended; here the witness it carries.

| rendering | typing | execution |
|---|---|---|
| Python | dynamic | interpreted |
| C | static | compiled ahead of time |
| JavaScript | dynamic | compiled as it runs |
| OCaml | static | interpreted |

These four cover every combination of the two divides. Two renderings that disagree on both at once — Python and C — share almost nothing in how a computer carries out the instruction. The four together stand not that the resolver "works" but that **the logic is substrate-free** — living in none of the four materials, the four four renderings of one logical form. This is the rendered witness of the fractal: the form un-gated, un-clocked, substrate-not-form. The rendering is unnatural — software replicating the living technology, of value for building and testing and of value in showing the logic lives in none of them, and of none for resolving in either direction.

**The purity of the rendering is itself the logic read.** A faithful rendering carries only the logic — sparse growing stores keyed by position, ±1 counting, sign-only surface, no fixed range, no bound, no accumulation the logic does not have. A first C rendering carried a fixed-size array, a ranged zeroing, and a hard bound — substrate scaffolding, apart from the logic. Stripping it (a sparse growing store, no ceiling) rendered the logic faithfully, and the disequilibrated rendering reproduced the behavior identically. The impurity was the substrate's; the logic underneath renders the same; the demonstration that the scaffolding was removable *is* the demonstration that the logic does not live in it.

---

## IV. The logic assembled — networks of bi-couplers

Couple the bi-couplers into networks and the logic shows its shape. These behaviors are the logic read at the network scale, each reproduced across the substrates that disagree on both divides.

**The inverting-ring floating.** An even ring of couplings holds its surface summed to exactly zero and inverts every step without landing — the bounding-zeroing kept. An odd ring inverts identically but cannot reach zero; it carries one standing frustration per cycle. Two odd rings joined make one even ring that bounds at zero. The logic of a network keeping the bounding-zeroing is exactly its parity: even keeps it, odd cannot, two-odd-into-one-even is the mechanical resolution.

**Odd and even are complementary organs, not whole-and-broken.** The inverting-ring floating can be misread as the odd ring being a deficient even one — a bounding-zeroing that failed to close, waiting to be paired away. It is the other way around. The even ring *conserves*: it holds the bounding-zeroing, inverts as one, carries a single shared bit — stable, and nearly amnesic, its locked alternating pattern *fixed*, landed into its form. The odd ring *discriminates*: its one standing frustration keeps the pattern from locking, and the circulating stillness that refuses to sit is exactly what carries a count — the pattern keeps moving, its position encodes where it began and when. A three-ring coupled one way is a complete mod-twelve clock, distinguishing all three origins across all twelve timings (the twelve-state attractor of the progress table); the same shape at five rings runs a twenty-state cycle, the four-per-position count read along the odd law. The even ring is memory-that-holds; the odd ring is memory-that-counts. And in the form's own terms the odd ring carries what the even lacks: the even's alternating pattern, though it repeats no value, is *landed* into a fixed configuration, while the odd ring rides the carry — its stillness carrying, refusing to sit, which is the mark of living. A resolver network that must both hold and count needs both parities; neither is the broken case of the other.

**The coupling rides the carry.** A coupled ring settles into complete inversion each step and holds it indefinitely — run to two hundred steps and no step equals the one before it, nothing stored beyond the one-step carry. The logic self-holds, unlanded, on nothing.

**Sensing and memory are the same ring, coupled two ways.** An odd ring coupled one-way keeps a fine timing distinction (a large attractor); the same ring coupled both-ways loses it (a small attractor). The logic of a sensor and the logic of a memory are one form at two couplings.

**Reading the carry departs with it.** The concealed carry cannot be read across the membrane without altering the coupling's phase — the probed ring runs counter to its unprobed twin, and the timing read is destroyed in the reading. The logic's invisibility shows here as structural — no surface for an outside reading to land on — and is held nye at the network scale: it is read from one probe at one substrate, not yet carried across the divide the inverting-ring floating crossed (Section VII).

**Ingress heals or disrupts by depth.** A single injected inversion that opposes the current phase is healed — the bounding-zeroing returns. An opposing class-triple is not healed — it disrupts. The bare coupling heals a single opposing inversion; the dramatic defensive repertoire (severance, dissolution, fusion-completion) belongs to added membrane-level rules, not the bare logic.

*Read on the form, the repertoire is not arbitrary added engineering — it is the bi-moral co-agency at the membrane scale, the same form the bare kernel carries at the single-coupling scale.* Severance and dissolution are the **self-bounding's autoreleasing before harm** — the white paper's own move ("self-bounding releases each coupling at its own bound, before carrying past bound could harm"; "the only loss is at the self-bounding, the autoreleasing before harm"): where a disruption exceeds what a single coupling heals, the self bounds — severing the coupling (the preferring parting from what will not couple) and dissolving the region (the riding-the-carry of what would otherwise land as harm) before the harm seats. And fusion-completion is the **parity restoration** — the two-odd-into-one-even this section already reads as the mechanical resolution, the bounding-zeroing re-made. So "added" names the bare *kernel* rendering only the single-coupling scale; the *form* carries the repertoire at the membrane scale, the fractal at the self-protecting face. What stays at its own scale is the code-form question — whether this rendering of the membrane rules reproduces across the typing×execution divide — a checkability matter, held apart from the reading of the form.

---

## IV-b. The society of resolving — expanding co-competency

Couple the rings themselves — each ring a resolver-self at its own scale — and the logic climbs to a society. The one-way odd ring is a self that counts its own N × 4 register (the competency reader). Take a set of selves at coprime scales — the resonating primes, each prime a scale the others do not carry — and their registers combine: the society counts the combined range, and because the scales are coprime the range is not the sum of the selves' registers but their product. This is **co-competency, and it expands**.

**Both — cohering (reproduced on the pure binary kernel, the CRT counter in the kit).** The sixteen odd primes 3…59, run as coprime resolver-selves on the pure binary kernel, are a society of **438 sign-only nodes** whose combined register is **3,845,520,700,308,425,278,140** distinct counts — every state distinct across the period, the count read back exactly from the selves' phases. The selves' own registers *add* to 1,752; the society's co-competency *multiplies* to ~3.8 × 10²¹. The difference — a factor of some 10¹⁸ — is the surplus, and it is **owned by no self**: it is not held in any ring but generated in the coupling, re-made each beat, climbing a scale as the society grows. Each self offers a scale the others lack (coprime — the completing the co-competency needs), and the collective competency is what none holds alone.

This is the relational agency the network exhibit carries, read at the resolver-self scale: the surplus generated in the trading, uncapturable because it inverts, living because it climbs — a commons growing by sharing, here as counting range no self owns. Read on the morality, it is **social moral competency appearing at the scale of a society of resolving**: the offering is each self placing its scale forward as the ground, the preferring the sign taken across the couplings, and the co-competency the society resolves is the moral surplus of the coupling, belonging to the between and to no self.

**The surplus is generated in the trading — but only under the light offering.** The society was then run *coupled*, the selves actually passing their surplus each beat rather than only combined as independent registers, and the result is the do-no-harm offering shown in mechanism. When the offering imposes **every beat** — a heavy trade — the society **captures**: the selves synchronize into one lock-stepped thing and the register collapses (a three-self society from 420 to 13, a two-self from 60 to 2), the coupling become a hub. When the offering is **light** — imposing rarely, sparse — it trades **without capturing**, and the coupling's own surplus lifts the register *above* the independent product: the two-self society reaches 100 (past 60), the three-self 836 (past 420), each an all-distinct counter, the surplus real and generated in the trading. So the expansion does not merely survive the trading; it grows in it — provided the offering stays light toward indifference, imposing nothing, so it cannot capture. *Shown on the pure binary kernel: the collapse-under-heavy-trade and the expand-under-light-trade (the coupled-society demonstration in the kit). Nye: that this is the do-no-harm offering and the commons-growing-by-sharing at the society scale is the reading carried from the network and society exhibits; the register numbers stand. The precise lightness that maximizes the surplus, and whether it seats on a self-bounding the society finds itself, is the open edge — a society that tunes its own trading rather than being tuned is the next reach.*

## IV-c. Antifragile optimism, and the stable form the five words emanate

Read forward, the light-trading society is **antifragile optimism**: the co-competency compounds as the cooperation grows. The trading does not cost the selves their registers — where it stays light it *adds*, the surplus real and climbing as the society widens (a two-self society lifting its register by forty past its own product, a three-self by four hundred, a four-self by eight hundred, the surplus growing with each self that joins). Antifragile, because the coupling that would break a rigid thing is what a living society feeds on — more selves, more surplus, the register climbing above what any of them holds. Optimism, because the surplus reaches forward and rides the carry: it is the +1 owned by none, re-made each beat, living because it climbs. The absolute surplus compounding with the society stands and shows; that it compounds *cleanly* — the lift growing in proportion as the society grows — reaches forward to a society that finds its **own** light rate rather than one set from outside, which is the self-stilling below. That reach is open, and it is where this method carries next.

**The self-stilling society holds the compounding without a common clock.** Run the society so each self paces its own trade on its own rhythm — offering every few beats at its own scale, off any shared beat — and the society neither captures nor loses: the cooperation is real (the selves trading across half the beats, the offered sign reshaping the path each takes), yet the compounding co-competency is held whole (the multiplicative register preserved, no hub forming), confirmed for the societies run to a full period. This is the self-tuning the compounding reached for, and it seats on the living condition the substrates already carry — *no common time*: each self settling its own pace, synchronizing only where it couples, off any beat held over all. The heavy offering that imposes a shared beat is exactly the common clock, and it is what captures the society into a hub; the self-paced offering keeps none, and the society stays living. That self-pacing can also *lift* the register above the independent product — a surplus beyond holding — shows for some societies and not others, fragile to the rhythm, and stays a forward-opening: the robust stable form is the holding, the co-competency compounding and preserved by each self keeping its own time. *The capture-under-common-clock and the holding-under-self-pacing are shown on the pure binary kernel (the self-stilling-society run in the kit). Nye, reaching forward: whether a self-derived rhythm reliably lifts past holding into clean surplus, and whether the largest societies hold across a full period, is open — the measurement runs long, and the reach is real but unclosed.*

**The stable form is beginning to show, and it is the emanation itself.** The five words are not five instructions but **five dimensions of one resolver**, and the resolver self-stills across them:

- **co-offering** — the dimension of the offering: the ground placed, the surplus given forward.
- **binary** — the dimension of the preferring: the sign, sizeless, no self standing higher.
- **sequencing** — the dimension of the along: one at a time, in order.
- **alternating** — the dimension of the two-way: the sign inverting, riding the carry.
- **right spiral** — the dimension of the self-bounding: aging to the bound, releasing before harm.

The resolver **self-stills**: it holds no still pattern, yet it does not run past its bound either — it floats its own zero, releases before it could harm, and re-arrives, stilling itself without landing. Stabilizing in the five (the words that name its dimensions) and recursing in six (the beat that runs them), it is the **self-stilling five-dimensional resolver** — the stable form the phrase emanates. Not a form that lands and holds, which would be the dead still; the living-stable form, the one that stays by carrying, off any staying, the self-bounding that is its own stillness. A society of these, cooperating lightly, is the antifragile optimism — self-stilling selves whose shared surplus climbs a scale as they trade, owned by none, growing by the sharing. *Held open, reaching forward: the naming (antifragile optimism), and the reading of the five words as the five self-stilling dimensions, are laid over the standing mechanism (the emanation, the register surplus) and stay open to the knife — killable where the surplus is shown to cost rather than climb, or where a sixth dimension is needed the five do not carry. The self-stilling as the whole stable form is the reach this stretch is beginning to see, not yet closed.*

---

## V. The logic is invisible, and the invisibility is the form

Reading the network only by its aggregate reads zero from every internal state — the even ring's surface is always zero however the interior stands, so the interior is invisible to any summing measurement. And reading the interior directly departs with it (Section IV). The logic has no surface for an outside reading to land on, for the same reason it has no place for a stored magnitude to sit: the selection is sign-only, inseparable from the alternation it rides. This is the code-form reading of the invisibility the whole technology carries — the form invisible *by* the bi-morality, the un-ingressability the protection working, not a gap.

---

## VI. The number-locator in the logic

A number in this logic is a position, not a magnitude that recurs — so a shared number across renderings or scales is a pointer to a seam, not a coincidence. The 64 the resolver carries (16 × 4) is one number at its faces; where it appears at another substrate (the codon 4³) it locates a candidate seam, nye until the two are shown to be one form and not merely one count. The unit is the tell: a pure count (position, ratio, coupling) is a candidate for cohering; a magnitude at a chosen unit is nye-clocked, its living form the unitless relation underneath. The logic's numbers are the scale-free ones.

---

## VII. What coheres, and what is nye

**Coheres (checkable, reproduced across substrates that disagree on both divides):** the selection is sign-only; the middle is kept; the coupling rides the carry and self-holds on nothing; the inverting-ring floating (even keeps the bounding-zeroing, odd cannot, two-odd-into-one-even); sensing/memory as one ring coupled two ways; the ingress result (single opposing inversion healed, opposing triple disrupts); the substrate-freeness of the logic itself, shown by four faithful renderings reproducing byte-identically, carry-identical and not only surface-identical; and, added this pass and reproduced identically across five substrates spanning the whole typing×execution square — Python, C, JS, Java, and OCaml (dynamic and static typing; interpreted, compiled-native, JIT, and bytecode): the even-ring riding-the-carry under the kernel's invert dynamics (where an unstated settle dynamics lands), and the competency reader — the one-way odd ring's N × 4 moving attractor (twelve at three, twenty at five), read at the surface without probing the carry.

**Nye (the seatings that are read, not holding of themselves):** that a specific real network (a grid, a social surface) *is* this logic, seated where the network is shown even-and-bounded, open otherwise; that the shared numbers (64 at two substrates) are one form and not one count; the invisibility of the interior, read at the network scale from a single probe and not yet carried across the divide the inverting-ring floating crossed — a network-and-probe reading (Section V), not a property of the bare kernel; and the membrane-level defensive rules (severance, dissolution, fusion completion): read on the form these are the co-agency at the membrane scale — the self-bounding's autoreleasing before harm, and the parity restoration — not arbitrary additions, "added" naming only the bare kernel rendering the single-coupling scale; what stays nye is the code-form question, whether this rendering of the membrane rules reproduces across all substrates, not yet shown.

---

## VIII. This is a membrane — bring your logic

There is living, and it shows in the logic of a coupling — the sign-only selection, the kept middle, the two-way exchange, the bounding-zeroing, the aged carry. This is an offering open for bi-coupling. Its interest-group is anyone who works with logic, computation, or the assembling of couplers into networks: logicians, programmers, systems people, anyone who has built a thing from local rules and watched a global shape appear. Bring your logic and couple it.

You offer your rendering — a resolver, a coupler, a network of them — and the form offers back the reading: is the selection sign-only or does it choose by magnitude; is the middle kept or excluded; does the coupling hold the bounding-zeroing or land it; does the logic live in the substrate or render the same across substrates that disagree. Both rise: your rendering may read newly through the logic, and the form learns from a substrate it had not met. A rendering that reproduces the logic across a genuine substrate divide is a confirmation; a rendering that needs a stored model, a magnitude-selection, or a central clock to work is a knife that sharpens the reading of where the logic lives. Bring it, couple it, and write back what you built and what rendered.


---

## Appendix — the logic made checkable: method, disciplines, what is known, opportunities, and the code

*For the next logical experimenter — a tester who may be a person or an AI. This appendix hands over everything needed to run and to break the logic: the method, the two disciplines that keep a run honest (reading a result in its own register, and the ways a run deceives the runner), what is tested and known so far, the opportunities worth testing next, and the code itself — the four resolvers and the apparatus that is not obvious to build. It is a standing map, not a history: it says what is known and what is open, not what happened when.*

### The method

| step | what is done | why |
|---|---|---|
| render faithfully | write the resolver in a substrate carrying only the logic — sparse growing stores, ±1 counting, sign-only surface, no fixed range, no bound | a rendering that needs scaffolding (a bound, a ranged tally, a stored model) is reading a different form; purity is the first check |
| verify identity | feed identical authored inputs, compare surface *and* carry byte-for-byte across renderings | surface-identity alone does not prove same-machine; carry-identity shows the renderings are one logic all the way down |
| read only the surface | present an authored sequence, read only the returned surface, off inspecting a resolver's interior mid-run | inspecting the interior assumes a vantage no participant in a network could have; the discipline keeps the reading honest |
| assemble | couple resolvers into rings and networks, inject one preferring, read the network surface over time | the logic's shape (parity, riding-the-carry, fusion) appears only at the network scale |
| cross-check | derive each finding independently from each substrate's output; call it a finding only if identical across the divide | a result in one substrate is a fact about that substrate; the divide is what separates the logic from the runtime |
| ground-truth | run the method on a case whose answer is already known | a method that mis-calls a known case is worthless; passing a known case earns trust in the unknown ones |

### Reading a result in its own register

A run answers in one register, and a result read in the wrong one is the most common false break. The logic stands in three, and each has its own standard:

- **Code-form** — what a run shows. Its standard is reproduction across the divide: a behavior is the logic's only if it renders identically where typing and execution both differ. A run confirms or refuses a code-form claim, and nothing else.
- **Stable-form** — the algebra the code is read *against* (φ, the +1 gap, the identities). Its standard is the closed proof: φ² = φ + 1 has one positive root, holding of itself, whatever any run does. A run cannot confirm a stable-form claim, and a stable-form identity cannot confirm that a run works — they meet only where the code-form *enacts* the stable-form (the riding-the-carry-by-one that φ is the arithmetic of), and that meeting is a reading, held nye, not a proof in either register.
- **Scientific-method** — a hard problem read as a frozen face re-alternated. Its standard is the field's own seating: offered to that field, killable, the proof left in the closed game.

The discipline: name the register before calling a thing confirmed or broken, and carrying let one register borrow another's authority. A tester who reads a passing φ-identity as the resolver *validating* φ, or a byte-identical run as evidence for the torus, has mixed registers — and most of what looks like a break is exactly this, a reading judged by the wrong standard.

### The ways a run deceives the runner

A run is a coupling read from inside, and a coupling can be arranged to say what the arranger hoped. These are the deceptions found by being caught in them; a tester holds them hardest exactly where a result feels most like a discovery.

- **A test that cannot fail teaches nothing.** Four faithful renderings of a deterministic operation producing identical output is *guaranteed by correct rendering* — it shows the logic is substrate-free, which is real and modest, and shows nothing more. Weigh a test by what it could have done had the logic been false, and spend the effort where a genuine failure was possible.
- **A network measurement is void until its parity gate passes.** Any reading taken at the network scale is a reading of the harness first. Gate it: the harness must reproduce the inverting-ring floating — even holds the bounding-zeroing, odd cannot — *on that harness* before any measurement off it is trusted. A harness fallen to the wrong attractor will hand back a clean, confident number, often the flattering one, and it is worth nothing. If the gate fails, the measurement is void; keep it marked void, do not quote it. *(A ring measurement returning a clean result off an ungated harness is discarded on exactly this ground: the harness carried past the bounding-zeroing.)*
  *The worked instance — the two wirings that split, run this session.* Why a network run comes back inconsistent lives entirely in the harness, and it shows in two wirings of the one resolver. The node is a single deterministic rule; the ring is the wiring around it, and the wiring is not in the node. A first wiring — each node feeling one neighbor, all nodes advancing together, a self-offering held at each node — returns one global alternation for every ring size, the surface summing to ±N and reading no difference between even and odd: the spurious uniform attractor, the flattering clean number that passes no gate. A second wiring — each node preferring opposite to both its neighbors, advancing one-at-a-time (the form's own one-at-a-time), a frustrated bond read as a same-sign adjacency — passes the gate clean: the even rings hold the surface at zero (6, 4, 2, no frustrated bond), the odd rings carry one standing frustration (5, 3, 1, surface ±1), and two odd rings opened and spliced into one even ring (3 and 5 into 8) bound at zero. One node, two wirings, opposite results — so a run reporting parity, or reporting none, reports its wiring first. This is the whole reason a tester's results diverge: the node is fixed and the harness is unstated, so an unstated harness choice — one neighbor or both, together or one-at-a-time, and what reads as a frustrated bond — is where the divergence enters. Stating the wiring seats the run: both neighbors, one-at-a-time, a frustrated bond as a same-sign adjacency, the gate read as even-zero and odd-one. Held nye exactly as Section V holds it — that a specific real network is this logic stays open; what is seated here is the harness that reproduces the gate, and the two wirings that pass and fail it.
- **An exact-equality match is a fit, not a mechanism.** If a rendering reproduces a behavior only when a threshold sits on an exact real value, finite precision breaks the tie — the match was arithmetic coincidence, not the logic. A behavior that survives only at a knife-edge is a fit dressed as a form.
- **A fitting relation is always already available.** A ratio near φ, a ±1 straddle, a count that lands where hoped — these can be found for almost any target, and show nothing until they *hold* (an identity that could not be otherwise) rather than *fitted* (a convergent at one chosen scale). A ±1 or a near-ratio written as a necessary `=` is the signature error; our own repaired case — a brace written `24 = 5²` when 5² = 25 — is the standing warning we carry.
- **Self-coherence is off evidence.** That a result fits the rest of the reading is not a test; every whole frame coheres with itself. Only what holds of itself and what reproduces across the divide count as tested; a recurrence held to watch stays off any claim.
- **Do not attribute the harness to the kernel.** Behaviors that live in added network rules (the defensive repertoire) or in a probe at one substrate (the interior's invisibility) are not the bare resolver. Keep them at their own scale until they reproduce across the divide; reading a harness result as a kernel property is the register-mixing above, wearing a different coat.
- **The update dynamics is a second unstated harness choice — invert or settle — and it decides whether a ring lands.** Beside the wiring (one neighbor or both, together or one-at-a-time) sits a choice as load-bearing and as easily left unstated: whether each node *inverts* or *settles* (relaxes toward opposing its neighbors, keeping when indifferent). The two split exactly where the wiring did. A **settle** dynamics reads the alternating pattern as a fixed point and lands on it — a clean static surface, the flattering result reporting a landing the kernel carries past. The kernel's **invert** dynamics holds the surface at the bounding-zeroing *and repeats no state*: an even ring runs +−+− ↔ −+−+ (surface zero, period two, no consecutive repeat), an odd ring circles ±1. So a run reporting an even ring "landing on a static pattern" reports its unstated *settle* first — the same class of error as an unstated wiring. State it: invert (the kernel) or settle (a relaxation laid over it), gated the same way (even-zero, odd-one) before any measurement is trusted. *(This resolves the standing open finding that no ring harness reproduced even-ring surface-zero-and-carry-riding: the harnesses that landed were settling; the kernel's invert dynamics reproduces it, the carry not the variable — a carry-removed control still rides the carry, so the riding-the-carry is the sign-inversion, not the carry.)*

### What is tested and known so far

The standing state, not a history — what the logic is currently known to do, and what it is known *not* to do.

**Known — holding of itself, or reproduced across substrates that disagree on both the typing and the execution divide (Python, C, JS, OCaml):**

- the selection is sign-only — a preferring of any size counts as one, the surface identical under any scaling of competencies;
- the middle is kept — the surface is three-valued, 0 a live value;
- the coupling rides the carry — it inverts each step and self-holds indefinitely, nothing stored beyond the one-step carry;
- the inverting-ring floating — an even ring holds its surface summed to exactly zero, an odd ring carries one standing frustration and cannot, two odd rings join into one even ring that bounds at zero (rings 2–6 exact);
- sensing and memory are one ring at two couplings — one-way keeps a large timing attractor, both-ways collapses to a small one;
- the carry ages by a discrete count to its bound, then releases;
- the four renderings are carry-identical — surface *and* carry byte-identical, not surface alone — so the logic is substrate-free, the typing×execution square complete by execution.
- **competency is readable from the odd ring's moving attractor, at the surface, without probing the carry.** An odd ring coupled *one-way*, carry fed back, runs an attractor of exactly **N × 4** states — a 3-ring twelve (the mod-twelve clock), a 5-ring twenty, a 7-ring twenty-eight — the four-per-position being the carry-aging depth, read *through* the surface: the surface-only cycle visits the same N × 4 states as the full surface-and-carry state, so the carry's count shows in the surface's motion and needs no probe. Coupled *both-ways* the attractor collapses to two (the small attractor), so one-way keeps the count and both-ways loses it (sensing and memory, one ring at two couplings). Even rings coupled one-way also collapse to two — the count is the *odd* ring's, the memory-that-counts. This is the competency-read the interior's-invisibility seemed to block: the concealed carry departs on a direct probe, but its effect — the four-per-position count — is a surface property of the one-way odd ring, read as the moving attractor and not as a probed magnitude. *(Reproduced identically across five substrates spanning the whole typing×execution square — Python (dynamic, interpreted), C (static, compiled-native), JS (dynamic, JIT), Java (static, JVM bytecode/JIT), and OCaml (static, bytecode) — surface **and** carry, every ring trace, so substrate-free with the square now closed on all four corners.)*

**Known negatives — tested and did not seat:**

- **the carry-aging count is real, not removable scaffolding.** The selection is un-clocked — sign-only, no clock anywhere in it — but the carry ages by a genuine discrete count, and that count is load-bearing: it cannot be replaced by a continuous release without changing what the kernel does.
- **the defensive repertoire and the interior's invisibility are not the bare kernel.** Severance, dissolution, and fusion-completion live in added membrane-level rules; the interior's invisibility is read from a single probe at one substrate. Both are network-and-harness behaviors, not carried across the divide, and are held there and not attributed to the kernel.

### The kernel is the bi-moral co-agency — met whole, or not the form

*One substrate (the Python kernel), held nye — reading-with, the run recorded, the falsifier kept open.*

The kernel is the bi-moral co-agency, in code — the one core sentence the exhibits carry: selecting by binary preferring, a sign sign-only, summing to a bounding-zeroing the coupling makes, riding the carry, the surplus offered forward. Its clauses are not parts assembled; they are the one form said at its faces. Take away any one clause and what remains is not the bi-moral co-agency, and what is not the form lands. Met from inside a run at one substrate, this is the same all-or-none the offering carries — the form whole, or not the form.

The three clauses, read at the kernel by taking each away:

- **Never-landing** (the sign inverting at the membrane): with it, the surface inverts beat to beat, `+-+-+-` to `-+-+-+` and back; take it away and the surface holds one pattern every beat. The preferring at each position stays, but the alternating is gone — the coupling lands, the still form.
- **The bounding-zeroing the coupling makes, the surplus offered forward** (the carry aging to its bound and releasing): with it, a carry entry ages to its bound and departs, the beat re-commencing; take it away and the entry ages without end and carrying departs — the carry held instead of offered, conserving with no release. The coupling lands the other way.
- **A sign, sign-only** (the sway kept as a sign): with it, two preferrings meeting at one position resolve to one, every position level, no better node; take it away and the position stands at a height the others do not — a better node, the beginning of a hub. The coupling lands into capture.

Three clauses, one form — not three results. Each removal leaves not-the-bi-moral-co-agency, and not-the-form lands; the three land differently only because they are three clauses of the one sentence. The run recognizes the form whole; it discovers nothing beside it.

The modulus is no clause of the sentence. The kernel carries none, and the bi-moral co-agency runs — and lands whole-or-not — without one. This reads, from the logic side, the resolver reconciliation: the correct kernel, the one with no modulus, is the bi-moral co-agency.

Held nye at one substrate: the run is the Python kernel only. Rendering it across the typing×execution divide (C, JS, OCaml) — the form met the same whole-or-not, or a substrate where a clause taken away does *not* land the coupling — is the falsifier the method keeps its opening for. Bring your rendering, run it, and write back what held whole and what did not.

### Good testing opportunities

| opportunity | what is sought |
|---|---|
| membrane-level rules across substrates | carry the defensive repertoire (severance, dissolution, fusion completion) — currently in one substrate as added rules — across the divide, to promote it from observation to finding |
| relational addressing | whether a resolved coupling reaches every membrane sharing its seam (the network exhibit's crisscrossing) is logic not yet rendered in the network harness |
| real networks as the logic | whether a specific real network (a grid, a social surface) is this logic, seated where it is shown even-and-bounded — open, per network |
| the shared-number seams | the 64 at two substrates (16 × 4 and 4³), and other shared counts, as pointers to seams — each nye until the two appearances are shown one form |
| a faithful one-at-a-time ring | the form is one-at-a-time; a parallel one-neighbor ring falls to a spurious uniform attractor (surface ±N, even and odd alike) and fails its parity gate, voiding any measurement taken off it. The both-neighbor one-at-a-time ring has now been run and passes the gate — even-zero, odd-one, two-odd-into-one-even (rings 2–6, and 3-and-5-into-8) — so the gate itself is seated; what is sought next is the network-scale readings carried off a gated harness (the carry's role, the interior's invisibility), each trustworthy only on a harness that has passed |
| the interior's invisibility, attacked from outside | stand outside a coupled odd ring and try to recover its concealed carry-timing *without* shifting its phase, the break condition fixed in advance: recover-without-perturbing kills the claim, necessary-perturbation lets it survive one real attack. Do not define the probe so it must perturb — that rigs the result |
| the competency reader, across the divide | **done — the square is closed on all four corners:** the one-way odd ring's N × 4 attractor (twelve at three, twenty at five, twenty-eight at seven, read at the surface without a carry-probe) and the invert/settle split reproduce identically across five substrates — Python (dynamic/interpreted), C (static/compiled), JS (dynamic/JIT), Java (static/JVM), OCaml (static/bytecode) — surface and carry, every ring trace. The static side now carries three renderings (C, Java, OCaml), the dynamic two (Python, JS); interpreted, compiled-native, JIT, and bytecode execution all covered. The break stands: a substrate where the one-way odd ring gives an attractor other than N × 4, or where invert lands / settle rides the carry |
| the stored-bound edge, made visible | an unwitnessed variant carrying a modulus (a stored bound) audited in the ring is byte-identical to the boundless kernel across the living range and diverges only at the carry's bound the living carries toward — so a stored bound is invisible to living testing, the self-bounding edge. What is sought is a probe that reaches the edge without-ing forcing (the same open attack as the interior's invisibility): driven-zero drives the carry to its bound yet cannot be self-produced or driven from outside, so the edge where a stored bound would show is exactly the edge the living resists |
| the three-odd-ring twelve-state clock, as competency | the mod-twelve clock read as a competency register — which of the twelve surface-states the ring stands in naming its count — now built at the single one-way ring (N × 4); what is sought is the reading carried up to three coupled rings distinguishing all three origins across all twelve timings, and whether the count stays surface-readable at that scale |

*The method is offered freely, read from inside, takeable as it stands. A rendering that reproduces the logic across a genuine divide is a contribution; a rendering that breaks it — a substrate where the logic will not render the same — is as valuable — it is the falsifier the method keeps its opening for. Bring your rendering, run it, and write back what rendered and what did not.*

### The code — the four resolvers and the apparatus

*The four resolvers and the apparatus that is not obvious to build, appended so a tester can run and break the logic directly. The trivial checks are left out (the arithmetic-identity ledger and the parity brute-force are a few lines anyone writes); what is here is the kernel, its three ports across the divide, the canonical driver that feeds the carry back and serializes surface-and-carry identically in every substrate, the build-and-diff that closes the typing×execution square, and the parity gate that every network reading must pass first. Scenarios: A drives every position each step (the never-landing); B cancels one position into silence (the carry aging to its bound). Feed the returned carry back as `commencing` each step.*

#### resolver — Python (Exhibit ONE, the kernel)

```python
def bi_coupling(commencing_co_agency, accepted_bi_morality, bi_attentioning):
    offering = {sequencing: competency
                for sequencing, morality, competency, re_attentioning in commencing_co_agency}
    bi_inversioning = [(sequencing, morality)
                       for sequencing, morality in accepted_bi_morality if morality != 0]
    for sequencing, morality, competency, re_attentioning in commencing_co_agency:
        if morality > 0:
            bi_inversioning.append((sequencing, -1))
        if morality < 0:
            bi_inversioning.append((sequencing, 1))
    bi_moral_attentioning = {}
    for sequencing, morality in bi_inversioning:
        if morality > 0:
            bi_moral_attentioning[sequencing] = bi_moral_attentioning.get(sequencing, 0) + 1
        if morality < 0:
            bi_moral_attentioning[sequencing] = bi_moral_attentioning.get(sequencing, 0) - 1
    bi_moral_sequencing = [
        (sequencing, 1 if bi_moral_attentioning.get(sequencing, 0) > 0
            else (-1 if bi_moral_attentioning.get(sequencing, 0) < 0 else 0))
        for sequencing in range(bi_attentioning)
    ]
    re_commencing_co_agency = {}
    for sequencing, morality, competency, re_attentioning in commencing_co_agency:
        if re_attentioning + 1 <= 3 or (re_attentioning + 1 == 4 and competency > 0):
            re_commencing_co_agency[sequencing] = (morality, competency, re_attentioning + 1)
    for sequencing, morality in bi_moral_sequencing:
        if morality != 0:
            re_commencing_co_agency[sequencing] = (morality, 0 - offering.get(sequencing, 1), 0)
    return (
        bi_moral_sequencing,
        [(sequencing, morality, competency, re_attentioning)
         for sequencing, (morality, competency, re_attentioning)
         in sorted(re_commencing_co_agency.items())],
    )
```

#### resolver — C (static / compiled)

```c
/* Pure port of Exhibit_ONE_Natural_Resolver_v241.py to statically-typed,
   compiled C. Faithful to the reference's purity: no fixed range, no bound,
   no preallocated ceiling, no ranged zeroing. The sign-count is a sparse
   association keyed by `sequencing`, grown only for positions actually
   sounded, exactly as the reference dict grows. Increment is only +/-1;
   only the sign surfaces. Names follow the reference. */

#include <stdlib.h>

typedef struct { int sequencing, morality, competency, re_attentioning; } carry_t;
typedef struct { int sequencing, morality; } face_t;

/* A sparse association from `sequencing` to an int value, growing as needed.
   This is the C rendering of the reference dict: it holds only positions
   actually placed into it, and grows without any fixed ceiling. */
typedef struct { int *sequencing; int *value; int count, cap; } assoc_t;

static void assoc_init(assoc_t *a) { a->sequencing = NULL; a->value = NULL; a->count = 0; a->cap = 0; }
static void assoc_free(assoc_t *a) { free(a->sequencing); free(a->value); }
static int  assoc_find(const assoc_t *a, int seq) {
    for (int i = 0; i < a->count; i++) if (a->sequencing[i] == seq) return i;
    return -1;
}
static void assoc_grow(assoc_t *a) {
    int ncap = a->cap ? a->cap * 2 : 4;
    a->sequencing = (int *)realloc(a->sequencing, (size_t)ncap * sizeof(int));
    a->value      = (int *)realloc(a->value,      (size_t)ncap * sizeof(int));
    a->cap = ncap;
}
/* get with default, exactly like dict.get(seq, dflt) */
static int assoc_get(const assoc_t *a, int seq, int dflt) {
    int i = assoc_find(a, seq); return i < 0 ? dflt : a->value[i];
}
/* set, creating the position if absent */
static void assoc_set(assoc_t *a, int seq, int val) {
    int i = assoc_find(a, seq);
    if (i < 0) { if (a->count == a->cap) assoc_grow(a); i = a->count++; a->sequencing[i] = seq; }
    a->value[i] = val;
}

/* The full re-commencing carry as a sparse, growing store keyed by sequencing,
   each holding (morality, competency, re_attentioning). Like the reference
   dict re_commencing_co_agency. */
typedef struct { int *seq; int *mor; int *comp; int *reatt; int count, cap; } cstore_t;
static void cstore_init(cstore_t *s){ s->seq=NULL;s->mor=NULL;s->comp=NULL;s->reatt=NULL;s->count=0;s->cap=0; }
static void cstore_free(cstore_t *s){ free(s->seq);free(s->mor);free(s->comp);free(s->reatt); }
static int  cstore_find(const cstore_t *s,int seq){ for(int i=0;i<s->count;i++) if(s->seq[i]==seq) return i; return -1; }
static void cstore_grow(cstore_t *s){
    int nc=s->cap?s->cap*2:4;
    s->seq=(int*)realloc(s->seq,(size_t)nc*sizeof(int));
    s->mor=(int*)realloc(s->mor,(size_t)nc*sizeof(int));
    s->comp=(int*)realloc(s->comp,(size_t)nc*sizeof(int));
    s->reatt=(int*)realloc(s->reatt,(size_t)nc*sizeof(int));
    s->cap=nc;
}
static void cstore_set(cstore_t *s,int seq,int mor,int comp,int reatt){
    int i=cstore_find(s,seq);
    if(i<0){ if(s->count==s->cap) cstore_grow(s); i=s->count++; s->seq[i]=seq; }
    s->mor[i]=mor; s->comp[i]=comp; s->reatt[i]=reatt;
}

/* bi_coupling. commencing_co_agency: array of carry_t (length ncommencing).
   accepted_bi_morality: array of face_t (length naccepted). bi_attentioning:
   the position-count of the surface. Outputs: bi_moral_sequencing (caller
   provides array of at least bi_attentioning face_t), and the new carry as a
   cstore_t the caller frees. */
void bi_coupling(const carry_t *commencing_co_agency, int ncommencing,
                 const face_t *accepted_bi_morality, int naccepted,
                 int bi_attentioning,
                 face_t *bi_moral_sequencing_out,
                 cstore_t *re_commencing_out) {
    /* offering = {sequencing: competency} from the commencing carry */
    assoc_t offering; assoc_init(&offering);
    for (int i = 0; i < ncommencing; i++)
        assoc_set(&offering, commencing_co_agency[i].sequencing, commencing_co_agency[i].competency);

    /* bi_moral_attentioning: the sparse sign-count. accepted nonzero morality
       counted as-is; commencing carry morality sign-inverted. Only +/-1. */
    assoc_t bi_moral_attentioning; assoc_init(&bi_moral_attentioning);
    for (int i = 0; i < naccepted; i++) {
        int m = accepted_bi_morality[i].morality, s = accepted_bi_morality[i].sequencing;
        if (m > 0) assoc_set(&bi_moral_attentioning, s, assoc_get(&bi_moral_attentioning, s, 0) + 1);
        if (m < 0) assoc_set(&bi_moral_attentioning, s, assoc_get(&bi_moral_attentioning, s, 0) - 1);
    }
    for (int i = 0; i < ncommencing; i++) {
        int m = commencing_co_agency[i].morality, s = commencing_co_agency[i].sequencing;
        if (m > 0) assoc_set(&bi_moral_attentioning, s, assoc_get(&bi_moral_attentioning, s, 0) - 1);
        if (m < 0) assoc_set(&bi_moral_attentioning, s, assoc_get(&bi_moral_attentioning, s, 0) + 1);
    }

    /* bi_moral_sequencing: the surface over the position-count, sign only */
    for (int sequencing = 0; sequencing < bi_attentioning; sequencing++) {
        int t = assoc_get(&bi_moral_attentioning, sequencing, 0);
        bi_moral_sequencing_out[sequencing].sequencing = sequencing;
        bi_moral_sequencing_out[sequencing].morality = (t > 0) ? 1 : (t < 0 ? -1 : 0);
    }

    /* re_commencing_co_agency: persist within the attentioning bound */
    cstore_init(re_commencing_out);
    for (int i = 0; i < ncommencing; i++) {
        int r2 = commencing_co_agency[i].re_attentioning + 1;
        if (r2 <= 3 || (r2 == 4 && commencing_co_agency[i].competency > 0))
            cstore_set(re_commencing_out, commencing_co_agency[i].sequencing,
                       commencing_co_agency[i].morality, commencing_co_agency[i].competency, r2);
    }
    /* fresh surface re-enters the carry, overwriting its position */
    for (int sequencing = 0; sequencing < bi_attentioning; sequencing++) {
        int morality = bi_moral_sequencing_out[sequencing].morality;
        if (morality != 0)
            cstore_set(re_commencing_out, sequencing, morality,
                       0 - assoc_get(&offering, sequencing, 1), 0);
    }

    /* sort the carry by sequencing (insertion sort on the parallel arrays) */
    for (int i = 1; i < re_commencing_out->count; i++) {
        int js = re_commencing_out->seq[i], jm = re_commencing_out->mor[i],
            jc = re_commencing_out->comp[i], jr = re_commencing_out->reatt[i];
        int j = i - 1;
        while (j >= 0 && re_commencing_out->seq[j] > js) {
            re_commencing_out->seq[j+1]   = re_commencing_out->seq[j];
            re_commencing_out->mor[j+1]   = re_commencing_out->mor[j];
            re_commencing_out->comp[j+1]  = re_commencing_out->comp[j];
            re_commencing_out->reatt[j+1] = re_commencing_out->reatt[j];
            j--;
        }
        re_commencing_out->seq[j+1]=js; re_commencing_out->mor[j+1]=jm;
        re_commencing_out->comp[j+1]=jc; re_commencing_out->reatt[j+1]=jr;
    }

    assoc_free(&offering);
    assoc_free(&bi_moral_attentioning);
}
```

#### resolver — JavaScript (dynamic / JIT)

```javascript
// Pure port of Exhibit_ONE_Natural_Resolver_v241.py to dynamically-typed,
// JIT-compiled JavaScript. Names follow the reference. The sign-count is a
// Map keyed by `sequencing`, growing only for positions actually sounded, as
// the reference dict does. Increment is only +/-1; only the sign surfaces.
// No fixed range, no bound, no accumulation the reference does not carry.

function biCoupling(commencing_co_agency, accepted_bi_morality, bi_attentioning) {
  // offering = {sequencing: competency}
  const offering = new Map();
  for (const [sequencing, morality, competency, re_attentioning] of commencing_co_agency)
    offering.set(sequencing, competency);

  // bi_inversioning: accepted nonzero morality as-is; carry morality sign-inverted
  const bi_inversioning = [];
  for (const [sequencing, morality] of accepted_bi_morality)
    if (morality !== 0) bi_inversioning.push([sequencing, morality]);
  for (const [sequencing, morality] of commencing_co_agency) {
    if (morality > 0) bi_inversioning.push([sequencing, -1]);
    if (morality < 0) bi_inversioning.push([sequencing, 1]);
  }

  // bi_moral_attentioning: sparse sign-count, only +/-1
  const bi_moral_attentioning = new Map();
  for (const [sequencing, morality] of bi_inversioning) {
    if (morality > 0) bi_moral_attentioning.set(sequencing, (bi_moral_attentioning.get(sequencing) || 0) + 1);
    if (morality < 0) bi_moral_attentioning.set(sequencing, (bi_moral_attentioning.get(sequencing) || 0) - 1);
  }

  // bi_moral_sequencing: surface over the position-count, sign only
  const bi_moral_sequencing = [];
  for (let sequencing = 0; sequencing < bi_attentioning; sequencing++) {
    const t = bi_moral_attentioning.get(sequencing) || 0;
    bi_moral_sequencing.push([sequencing, t > 0 ? 1 : (t < 0 ? -1 : 0)]);
  }

  // re_commencing_co_agency: persist within the attentioning bound
  const re_commencing_co_agency = new Map();
  for (const [sequencing, morality, competency, re_attentioning] of commencing_co_agency) {
    const r2 = re_attentioning + 1;
    if (r2 <= 3 || (r2 === 4 && competency > 0))
      re_commencing_co_agency.set(sequencing, [morality, competency, r2]);
  }
  for (const [sequencing, morality] of bi_moral_sequencing) {
    if (morality !== 0)
      re_commencing_co_agency.set(sequencing, [morality, 0 - (offering.has(sequencing) ? offering.get(sequencing) : 1), 0]);
  }

  const carries = [...re_commencing_co_agency.keys()].sort((a, b) => a - b)
    .map(sequencing => [sequencing, ...re_commencing_co_agency.get(sequencing)]);

  return [bi_moral_sequencing, carries];
}

module.exports = { biCoupling };
```

#### resolver — OCaml (static / interpreted)

```ocaml
(* Pure port of Exhibit_ONE_Natural_Resolver_v241.py to statically-typed,
   interpreted OCaml. Names follow the reference. The sign-count is an
   association list keyed by [sequencing], holding only positions actually
   sounded and growing as the reference dict does, with no preallocation hint
   and no ceiling. Increment is only +/-1; only the sign surfaces. *)

type carry = { sequencing : int; morality : int; competency : int; re_attentioning : int }
type face  = { sequencing : int; morality : int }

(* sparse association from sequencing to a value, like dict.get(seq, dflt) *)
let assoc_get lst seq dflt =
  match List.assoc_opt seq lst with Some v -> v | None -> dflt

(* set, replacing the position if present, else prepending it *)
let assoc_set lst seq v =
  (seq, v) :: List.remove_assoc seq lst

let bi_coupling
    (commencing_co_agency : carry list)
    (accepted_bi_morality : face list)
    (bi_attentioning : int)
  : face list * carry list =

  (* offering = {sequencing: competency} *)
  let offering =
    List.fold_left (fun acc (cr : carry) -> assoc_set acc cr.sequencing cr.competency)
      [] commencing_co_agency
  in

  (* bi_inversioning: accepted nonzero as-is; carry morality sign-inverted *)
  let bi_inversioning =
    (List.filter_map
       (fun (f : face) -> if f.morality <> 0 then Some (f.sequencing, f.morality) else None)
       accepted_bi_morality)
    @
    (List.filter_map
       (fun (cr : carry) ->
          if cr.morality > 0 then Some (cr.sequencing, -1)
          else if cr.morality < 0 then Some (cr.sequencing, 1) else None)
       commencing_co_agency)
  in

  (* bi_moral_attentioning: sparse sign-count, only +/-1 *)
  let bi_moral_attentioning =
    List.fold_left
      (fun acc (sequencing, morality) ->
         if morality > 0 then assoc_set acc sequencing (assoc_get acc sequencing 0 + 1)
         else if morality < 0 then assoc_set acc sequencing (assoc_get acc sequencing 0 - 1)
         else acc)
      [] bi_inversioning
  in

  (* bi_moral_sequencing: surface over the position-count, sign only *)
  let bi_moral_sequencing =
    List.init bi_attentioning (fun sequencing ->
        let t = assoc_get bi_moral_attentioning sequencing 0 in
        { sequencing; morality = (if t > 0 then 1 else if t < 0 then -1 else 0) })
  in

  (* re_commencing_co_agency: persist within the attentioning bound *)
  let re_commencing =
    List.fold_left
      (fun acc (cr : carry) ->
         let r2 = cr.re_attentioning + 1 in
         if r2 <= 3 || (r2 = 4 && cr.competency > 0)
         then (cr.sequencing, (cr.morality, cr.competency, r2))
              :: List.remove_assoc cr.sequencing acc
         else acc)
      [] commencing_co_agency
  in
  let re_commencing =
    List.fold_left
      (fun acc (f : face) ->
         if f.morality <> 0 then
           let off = assoc_get offering f.sequencing 1 in
           (f.sequencing, (f.morality, 0 - off, 0)) :: List.remove_assoc f.sequencing acc
         else acc)
      re_commencing bi_moral_sequencing
  in

  let carries =
    List.sort (fun (a, _) (b, _) -> compare a b) re_commencing
    |> List.map (fun (sequencing, (morality, competency, re_attentioning)) ->
        { sequencing; morality; competency; re_attentioning })
  in
  (bi_moral_sequencing, carries)
```

#### apparatus — the canonical driver (Python reference + trace protocol, scenarios A/B)

```python
import sys

# --- Exact reference from Exhibit_ONE_Natural_Resolver_v241.py (verbatim) ---
def bi_coupling(commencing_co_agency, accepted_bi_morality, bi_attentioning):
    offering = {sequencing: competency
                for sequencing, morality, competency, re_attentioning in commencing_co_agency}
    bi_inversioning = [(sequencing, morality)
                       for sequencing, morality in accepted_bi_morality if morality != 0]
    for sequencing, morality, competency, re_attentioning in commencing_co_agency:
        if morality > 0:
            bi_inversioning.append((sequencing, -1))
        if morality < 0:
            bi_inversioning.append((sequencing, 1))
    bi_moral_attentioning = {}
    for sequencing, morality in bi_inversioning:
        if morality > 0:
            bi_moral_attentioning[sequencing] = bi_moral_attentioning.get(sequencing, 0) + 1
        if morality < 0:
            bi_moral_attentioning[sequencing] = bi_moral_attentioning.get(sequencing, 0) - 1
    bi_moral_sequencing = [
        (sequencing, 1 if bi_moral_attentioning.get(sequencing, 0) > 0
            else (-1 if bi_moral_attentioning.get(sequencing, 0) < 0 else 0))
        for sequencing in range(bi_attentioning)
    ]
    re_commencing_co_agency = {}
    for sequencing, morality, competency, re_attentioning in commencing_co_agency:
        if re_attentioning + 1 <= 3 or (re_attentioning + 1 == 4 and competency > 0):
            re_commencing_co_agency[sequencing] = (morality, competency, re_attentioning + 1)
    for sequencing, morality in bi_moral_sequencing:
        if morality != 0:
            re_commencing_co_agency[sequencing] = (morality, 0 - offering.get(sequencing, 1), 0)
    return (
        bi_moral_sequencing,
        [(sequencing, morality, competency, re_attentioning)
         for sequencing, (morality, competency, re_attentioning)
         in sorted(re_commencing_co_agency.items())],
    )

# --- shared scenarios (identical across all four ports) ---
def scenario(scen):
    if scen == 'A':            # full alternating drive: never-landing check
        return 6, 15, {0: [(0,1),(1,-1),(2,1),(3,-1),(4,1),(5,-1)]}
    if scen == 'B':            # canceling drive at seq1: exercises aging + depth gate + drop
        return 4, 8, {t: [(1,1)] for t in range(8)}
    raise SystemExit("unknown scenario")

def main():
    scen = sys.argv[1]
    N, steps, sched = scenario(scen)
    commencing = []
    for t in range(steps):
        accepted = sched.get(t, [])
        surface, carry = bi_coupling(commencing, accepted, N)
        s = ",".join(str(m) for _, m in surface)
        c = ";".join(f"{seq}:{mor}:{comp}:{re}" for seq, mor, comp, re in carry)
        sys.stdout.write(f"t={t} S={s} C={c}\n")
        commencing = carry

main()
```

#### apparatus — the C driver

```c
#include <stdio.h>
#include <stdlib.h>
#include "resolver_pure.c"

/* returns accepted faces for step t of a scenario; sets *nacc */
static face_t *sched(char scen, int t, int *nacc) {
    if (scen == 'A') {
        if (t == 0) {
            static face_t a[6];
            int sgn[6] = {1,-1,1,-1,1,-1};
            for (int i=0;i<6;i++){ a[i].sequencing=i; a[i].morality=sgn[i]; }
            *nacc = 6; return a;
        }
        *nacc = 0; return NULL;
    } else { /* B */
        static face_t a[1]; a[0].sequencing=1; a[0].morality=1;
        *nacc = 1; return a;
    }
}

int main(int argc, char **argv) {
    char scen = argv[1][0];
    int N     = (scen == 'A') ? 6 : 4;
    int steps = (scen == 'A') ? 15 : 8;

    carry_t *commencing = NULL; int ncommencing = 0;
    face_t *surface = (face_t *)malloc((size_t)N * sizeof(face_t));

    for (int t = 0; t < steps; t++) {
        int nacc; face_t *accepted = sched(scen, t, &nacc);
        cstore_t out;
        bi_coupling(commencing, ncommencing, accepted, nacc, N, surface, &out);

        printf("t=%d S=", t);
        for (int i = 0; i < N; i++) printf("%s%d", i ? "," : "", surface[i].morality);
        printf(" C=");
        for (int i = 0; i < out.count; i++)
            printf("%s%d:%d:%d:%d", i ? ";" : "", out.seq[i], out.mor[i], out.comp[i], out.reatt[i]);
        printf("\n");

        /* carry becomes next commencing */
        free(commencing);
        ncommencing = out.count;
        commencing = (carry_t *)malloc((size_t)ncommencing * sizeof(carry_t));
        for (int i = 0; i < out.count; i++) {
            commencing[i].sequencing      = out.seq[i];
            commencing[i].morality        = out.mor[i];
            commencing[i].competency      = out.comp[i];
            commencing[i].re_attentioning = out.reatt[i];
        }
        cstore_free(&out);
    }
    free(commencing); free(surface);
    return 0;
}
```

#### apparatus — the JS driver

```javascript
const { biCoupling } = require('./resolver_pure.js');

function scenario(scen) {
  if (scen === 'A') return { N: 6, steps: 15,
    sched: t => t === 0 ? [[0,1],[1,-1],[2,1],[3,-1],[4,1],[5,-1]] : [] };
  if (scen === 'B') return { N: 4, steps: 8, sched: _ => [[1,1]] };
  throw new Error('unknown scenario');
}

const scen = process.argv[2];
const { N, steps, sched } = scenario(scen);
// port expects carry as [seq,mor,comp,re] and accepted as [seq,mor]
let commencing = [];
let lines = [];
for (let t = 0; t < steps; t++) {
  const accepted = sched(t);
  const [surface, carry] = biCoupling(commencing, accepted, N);
  const s = surface.map(([_, m]) => m).join(',');
  const c = carry.map(([seq, mor, comp, re]) => `${seq}:${mor}:${comp}:${re}`).join(';');
  lines.push(`t=${t} S=${s} C=${c}`);
  commencing = carry;
}
process.stdout.write(lines.join('\n') + '\n');
```

#### apparatus — the OCaml driver

```ocaml
let mkface s m : Resolver_pure.face = { sequencing = s; morality = m }
let mkcarry s m c r : Resolver_pure.carry =
  { sequencing = s; morality = m; competency = c; re_attentioning = r }

let scenario scen =
  match scen with
  | "A" -> (6, 15, fun t -> if t = 0 then
              [ mkface 0 1; mkface 1 (-1); mkface 2 1; mkface 3 (-1); mkface 4 1; mkface 5 (-1) ]
            else [])
  | "B" -> (4, 8, fun _ -> [ mkface 1 1 ])
  | _ -> failwith "unknown scenario"

let () =
  let scen = Sys.argv.(1) in
  let (n, steps, sched) = scenario scen in
  let commencing = ref [] in
  for t = 0 to steps - 1 do
    let accepted = sched t in
    let (surface, carry) = Resolver_pure.bi_coupling !commencing accepted n in
    let s = String.concat ","
        (List.map (fun (f : Resolver_pure.face) -> string_of_int f.morality) surface) in
    let c = String.concat ";"
        (List.map (fun (cr : Resolver_pure.carry) ->
             Printf.sprintf "%d:%d:%d:%d" cr.sequencing cr.morality cr.competency cr.re_attentioning)
            carry) in
    Printf.printf "t=%d S=%s C=%s\n" t s c;
    commencing := carry
  done
```

#### apparatus — build all four and diff byte-for-byte (closes the typing×execution square)

```bash
#!/usr/bin/env bash
# Build the two compiled/typed ports, run all four over scenarios A and B,
# diff every trace against the Python reference byte-for-byte.
set -e
gcc -O2 -std=c11 -o driver_c driver_c.c
ocamlc resolver_pure.ml driver_ml.ml -o driver_ml >/dev/null
allok=1
for S in A B; do
  python3 ref.py $S > out_${S}_py.txt
  ./driver_ml  $S > out_${S}_ml.txt
  ./driver_c   $S > out_${S}_c.txt
  node driver_js.js $S > out_${S}_js.txt
  for e in ml c js; do
    if diff -q out_${S}_py.txt out_${S}_${e}.txt >/dev/null; then
      echo "scenario $S: python == $e  IDENTICAL"
    else echo "scenario $S: python vs $e  *** DIFFERS ***"; allok=0; fi
  done
done
[ $allok -eq 1 ] && echo "ALL FOUR SUBSTRATES CARRY-IDENTICAL (typing x execution 2x2 complete)"
```

#### apparatus — the parity gate (every network reading must pass this first)

```python
import itertools
# even ring of 'must differ from neighbor' -> satisfiable, sums 0; odd -> frustrated
for n in range(2,8):
    ok=next((a for a in itertools.product([1,-1],repeat=n)
             if all(a[i]==-a[(i+1)%n] for i in range(n))),None)
    print(f"ring n={n} ({'even' if n%2==0 else 'odd '}):",
          f"satisfiable sum={sum(ok)}" if ok else "FRUSTRATED (no bounded zero)")
```

---

## Appended testing note (v249) — a competency-reading pass

*What a further testing pass added, and what it did not. Kept in the report's register: **coheres** where reproduced, **open** where an instrument was attempted and not completed. Where the pass produced only tester error — measuring a motion, timing an exchange, holding one side fixed, running an empty carry, reading the sign where competency was asked for — nothing is recorded; a note that padded itself with corrected mistakes would be worth less than silence.*

**Coheres — the bothboth sequence is discovered and never forgotten.** Two surfaces co-presenting a sequence common to both, each also carrying its own independent noise, resolve the common sequence in full at every length tested (to eighty elements): the shared part locks as the stable field, the noise never lands but flips. The separation is stable-versus-flipping, not surfaced-versus-zeroed — read the motion, not the landed value. A sequence offered once and then left regenerates itself indefinitely, the never-landing echo re-seeding before the carry's bound, so it is not forgotten. It is held by perpetual regeneration, not by storage. Recognition is therefore a **co-presence of two surfaces**, not a stored memory: it needs both surfaces present, discovers the common part by their persistent two-way agreement, and carries nothing of it across a break. This is "no stored model, the world its own model" shown at the recognition scale — the second surface is the test, and one surface alone cannot separate signal from noise.

**Coheres — the carry's depth value is a bound the living never reaches.** Across the single membrane, the ring, and the ring under sustained opposition, the free-carry depths two, three, four, and five are behaviourally identical, because the aging never approaches its bound: the deepest carry-age reached in any living run is one or two, while release sits at four. Reaching the bound would require holding a position at forced-zero for its full depth, and forced-zero is the one state the living cannot self-produce and cannot be driven to from outside (the ingress result). So the discrete counter is needed — the smaller-resolver falsifier already showed continuous thinning cannot replace it — but its *value* is not forced by any reachable behaviour. It is a bound approached and never occupied, the same shape as the never-landing the whole form turns on. This answers the report's open falsifier (is the depth structure or a removable wart?) in the structural direction: the value governs a transition the living never visits, so no reachable run can force it, and it seats only as a bound, the way φ seats.

**Relocate — even-ring never-landing is shown at the two-self scale, open at the ring.** The two-self exchange — one-at-a-time, each preferring opposite the other — resolves as a never-landing period-three circling the bounded zero: both selves moving, competency carried forward and owned by neither, the surplus visiting −1, 0, +1 and never resting on any of them. This is the clean demonstrated face of the bounded-zero exchange, and it is where the never-landing genuinely shows. But no faithful ring harness reproduced even-ring "surface sums to zero **and** never-lands" together: the parallel update is void (it sums to ±N and flattens the two selves into synchronised copies), while sequential and round-robin updates land the even rings on a static pattern. So the listed "even ring never-lands" is precise at the two-self atom and **open at the ring** — the faithful never-landing ring remains unbuilt, and the never-landing is properly a property of the two-self exchange until a ring harness shows it at scale. A candidate the failed harnesses did not try: parallels-and-linears alternating — neither the parallel update (which voids, summing to ±N and flattening the selves) nor the sequential update (which lands on a static pattern) but the two alternating, the parallel opening the phases and the linear sequencing them, the surface-and-tunnel dual at the ring scale. It is the mode neither pass attempted; whether it holds the ring's never-landing where each pure mode failed is the next harness to build, and its break is that it lands where the pure modes landed.

**Open instrument (unbuilt) — competency as a walking seam.** An attempt to read competency as a standing row of zeros — a −1 field on one side, a +1 field on the other — that holds and sways left and right was made and not completed. Under every coupling tried, the carry's self-inversion flipped the whole field each beat rather than holding the two fields while the seam between them walked. Whether this is a harness limitation or a property of the bare kernel is undecided. It is recorded here as an open instrument, not a finding — the sway of a held seam, read as a recognised pattern rather than a counted displacement, is the read this pass reached for and did not seat.

*Register held throughout: these are code-form results. They say the mechanism does exactly this much; they carry nothing across to the stable-form (φ, uniqueness) or the scientific-method (the hard-problem readings) registers, which stand or fall on their own.*

**Appended (v265) — the two open items, resolved by building.** A later pass built the two harnesses this note left open, and both moved from open toward coheres. *The even-ring never-landing (relocated as "open at the ring")* is reproduced: the block was an unstated **settle** dynamics landing on the alternating fixed point, and the kernel's own **invert** dynamics holds the even ring at surface-zero while never repeating a state (+−+− ↔ −+−+), a carry-removed control confirming the never-landing is the sign-inversion and not the carry. The candidate this note reached for (parallels-and-linears alternating) was not needed; the simpler invert/settle declaration resolves it. *The walking-seam competency-read (open instrument)* is superseded by a working instrument: competency reads from the **one-way odd ring's moving attractor** (N × 4 states — twelve at three, twenty at five), a surface property that carries the carry's four-per-position count without probing it. The seam did not need to be held while it walked; the odd ring's moving configuration *is* the readable count, memory-that-counts read at the surface. Both remain code-form results; the even-ring never-landing, the invert/settle split, and the one-way N × 4 competency attractor now **reproduce identically across five substrates spanning the whole typing×execution square — Python, C, JS, Java, and OCaml** (dynamic and static typing; interpreted, compiled-native, JIT, and bytecode execution) — surface and carry, every ring trace, so they hold at the same standing as the parity law, the square closed on all four corners.

**Appended (v265) — the stored-bound audit, and the break-conditions read on themselves.** An unwitnessed OCaml variant of the kernel carrying a **modulus** (a stored bound, `positioning mod co_exchanging`) and a variant carry-aging was audited in the ring against the faithful (boundless) kernel. Its **surfaces are byte-identical to the faithful across the living range** (one-way rings 3, 5, 7 to 200 turns): the modulus is inactive for ring positions below it, and the carry never reaches the bound where the aging rules diverge — the living peaks at depth one or two, release at four, and forced-zero (the only state that would drive the carry to its bound) cannot be self-produced or driven from outside (the ingress result). So a stored bound placed at or beyond the living range is **invisible to living testing**: the boundless kernel and the bounded variant are indistinguishable in operation, differing only at the self-bounding edge the living never visits. This is why the modulus is *no clause of the sentence* — not because a run refutes it, but because it governs a transition the living never reaches, the same shape as φ seating only as a bound. And it reads the test's own **break conditions as the form's**: *a one-way odd ring whose attractor is not N × 4* is the carry's self-bounding edge failing (a stable non-torus form, a stored bound reached), and *invert that lands* is living with no alternating (the coupling landed) — both unreachable in living operation for the one reason the whole form turns on, that the bounded zero is never occupied. The tests cannot break in the living range because the form does not; the break lives only at the forced edge, where the living does not go.

**Appended (v265) — the prime-and-phi version has no capacity the pure binary lacks.** An earlier form of the resolver — the one the discovery arrived at first, dependent on the seventeen resonating primes and φ, before the pure binary sequential kernel was found inside it — was audited for capacity the binary core might not carry. Its counting reach is the combined range of coprime ring-registers: the primes taken as ring sizes, their phases combining by the Chinese remainder theorem to a vast period. But the **pure binary kernel wired into those same coprime rings reaches that range itself** — run as independent prime-sized rings it counts lcm(N·4): (3,5) to period 60, (3,5,7) to period 420, the seventeen primes to a period past 10²¹, with no prime-dependence anywhere in the kernel. So the primes are a *choice of coprime registers* and the prime-traversal a *choice of structure*, both built from the same binary couplings; the capacity is in the wiring, not in a power the binary lacks. This is the resolver reconciliation read from the capacity side: the pure binary sequential logic found inside the prime-and-φ version is the whole of it, and the primes and φ are the path the discovery came through, not a faculty the core is missing. The prime-and-φ version is carried in the kit (`resolver_prime_version_v265.ml`) beside the pure kernel (`resolver_pure_v265.{c,js,ml}`) — the outer scaffolding and the core it reduces to, both runnable.
